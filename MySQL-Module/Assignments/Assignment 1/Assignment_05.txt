Assignment_no 05





Q1) Write an SQL query to list all books written by author 'Loni' with price less than 600.

--select *from book
where author = 'loni' and price < 600;




Q2) Write an SQL query to display issue details of books that are not returned yet.

--select *from issue
where return_date is null;




Q3) Write an SQL query to update all blank return_date values to '2025-12-31' excluding issue
IDs 7005 and 7006.

--update issue
set return_date = '2025-12-31'
where return_date is null
and issue_id not in (7005, 7006);





Q4) Write an SQL query to display issue details of books issued for more than 30 days.

--select *from issue
where datediff(current_date, issue_date) > 30;





Q5) Write an SQL query to list all books priced between 500 and 750 and belonging to category
'Database'.

--select *from book
where price between 500 and 750
and category = 'database';





Q6) Write an SQL query to list all books belonging to categories Science, Database, Fiction, or
Management.

--select *from book
where category in ('science', 'database', 'fiction', 'management');





Q7) Write an SQL query to list all members in descending order of penalty amount.

--select *from member
order by penalty_amount desc;




Q8) Write an SQL query to update book number 103 with cost = 300 and category = 'RDBMS'.

--update book
set cost = 300,category = 'rdbms'
where book_id = 103;




Q9) Write an SQL query to list all books sorted by category (ascending) and price (descending).

--select *from book
order by category asc, price desc;




Q10) Write an SQL query to list all books whose name contains the word 'SQL'.

--select *from book
where name like '%sql%';




Q11) Write an SQL query to display Lib_Issue_Id, Issue_Date, Return_Date and number of days
the book was issued.

--select lib_issue_id,
       issue_date,
       return_date,
       datediff(return_date, issue_date) as no_of_days
from issue;



12) Write an SQL query to list member details ordered by their joining date.

--select *from member
order by joining_date asc;




Q13) Write an SQL query to count total books issued to member ID 1.

--select count(*) as total_books
from issue
where member_id = 1;




Q14) Write an SQL query to display total penalty due for all members.

--select sum(penalty_amount) as total_penalty
from member;




Q15) Write an SQL query to display total number of members.

--select count(*) as total_members
from member;




Q16) Write an SQL query to display total number of books issued.

--select count(*) as total_books_issued
from issue;




Q17) Write an SQL query to display average membership fees paid by all members.

--select avg(membership_fee) as average_fee
from member;




Q18) Write an SQL query to display each category and number of books in that category.

--select category, count(*) as total_books
from book
group by category;




Q19) Write an SQL query to display book_no and number of times each book is issued, sorted
by count descending.

--select book_no, count(*) as issue_count
from issue
group by book_no
order by issue_count desc;




Q20) Write an SQL query to display maximum, minimum, total and average penalty amount
from Member table.

--select 
max(penalty_amount) as max_penalty,
min(penalty_amount) as min_penalty,
sum(penalty_amount) as total_penalty,
avg(penalty_amount) as avg_penalty
from member;





Q21) Write an SQL query to display member IDs who have issued more than 2 books.

--select member_id
from issue
group by member_id
having count(*) > 2;




Q22) Write an SQL query to display member_id, book_no and number of times the same book
was issued by that member.

--select member_id, book_no, count(*) as issue_count
from issue
group by member_id, book_no;





Q23)Write an SQL query to display month-wise count of books issued, sorted by count
descending.

----select month(issue_date) as month, count(*) as total_issued
from issue
group by month(issue_date)
order by total_issued desc;





Q24) Write an SQL query to list book numbers of books that have never been issued.

--select book_no
from book
where book_no not in (
    select book_no
    from issue
);





Q25) Write an SQL query to list member IDs who exist in Member table and have issued at least one book.

--select distinct member_id
from issue
where member_id in (
    select member_id
    from member
);





Q26) Write an SQL query to display member ID with highest and lowest number of books
issued.

--(
select member_id, count(*) as total_issued
from issue
group by member_id
order by total_issued desc
limit 1
)
union
(
select member_id, count(*) as total_issued
from issue
group by member_id
order by total_issued asc
limit 1
);




Q27) Write an SQL query to list issue details for books issued in December or July.

--select *
from issue
where month(issue_date) in (7, 12);





Q28) Write an SQL query to display Book_No, Book_Name and Issue_Date for books issued in
December and belonging to category Database.

--select b.book_no, b.book_name, i.issue_date
from book b
join issue i on b.book_no = i.book_no
where month(i.issue_date) = 12
and b.category = 'database';




Q29) Write an SQL query to display Member_Id, Member_Name and Max_Books_Allowed
sorted by Max_Books_Allowed descending.

--select member_id, member_name, max_books_allowed
from member
order by max_books_allowed desc;





Q30) Write an SQL query to list Book_No, Book_Name, Issue_Date and Return_Date for all
books issued by member 'Richa Sharma'.

--select b.book_no, b.book_name, i.issue_date, i.return_date
from book b
join issue i on b.book_no = i.book_no
join member m on i.member_id = m.member_id
where m.member_name = 'richa sharma';





Q31) Write an SQL query to list details of members who have issued books in Database
category.

--select distinct m.*
from member m
join issue i on m.member_id = i.member_id
join book b on i.book_no = b.book_no
where b.category = 'database';




Q32) Write an SQL query to list books having the highest price in each category.

--select *
from book b
where price = (
    select max(price)
    from book
    where category = b.category
);





Q33) Write an SQL query to list issue details where issue_date is outside the membership period
of that member.

--select i.*
from issue i
join member m on i.member_id = m.member_id
where i.issue_date not between m.joining_date and m.expiry_date;





Q34) Write an SQL query to list members who have not issued any book so far.

--select *
from member m
where m.member_id not in (
    select member_id
    from issue
);





Q35) Write an SQL query to list members who have issued the same book as issued by 'Garima
Sen'.


--select distinct m2.member_name
from issue i1
join member m1 on i1.member_id = m1.member_id
join issue i2 on i1.book_no = i2.book_no
join member m2 on i2.member_id = m2.member_id
where m1.member_name = 'garima sen'
and m2.member_name <> 'garima sen';




Q36) Write an SQL query to list book name and price of books not returned for more than 30
days.

--select b.book_name, b.price
from book b
join issue i on b.book_no = i.book_no
where i.return_date is null
and datediff(curdate(), i.issue_date) > 30;




Q37) Write an SQL query to list authors who have written more than one book.

--select author from book
group by author
having count(book_no) > 1;





Q38) Write an SQL query to display member(s) who issued the maximum and minimum number
of books.

--select m.member_name, count(i.book_no) as total_books
from member m
join issue i on m.member_id = i.member_id
group by m.member_id, m.member_name
having count(i.book_no) = (
    select max(cnt) from (
        select count(book_no) as cnt
        from issue
        group by member_id
    ) t
)
or count(i.book_no) = (
    select min(cnt) from (
        select count(book_no) as cnt
        from issue
        group by member_id
    ) t
);




Q39) Write an SQL query to list details of top 3 highest priced books.

--select m.member_name, count(i.book_no) as total_books
from member m
join issue i on m.member_id = i.member_id
group by m.member_id, m.member_name
having count(i.book_no) = (
    select max(cnt) from (
        select count(book_no) as cnt
        from issue
        group by member_id
    ) t
)
or count(i.book_no) = (
    select min(cnt) from (
        select count(book_no) as cnt
        from issue
        group by member_id
    ) t
);




Q40) Write an SQL query to calculate total cost of books currently issued but not returned.

--select sum(b.price) as total_cost
from book b
join issue i on b.book_no = i.book_no
where i.return_date is null;





Q41) Write an SQL query to display the book issued maximum number of times.

--select b.book_name, count(i.book_no) as issue_count
from book b
join issue i on b.book_no = i.book_no
group by b.book_no, b.book_name
having count(i.book_no) = (
    select max(cnt) from (
        select count(book_no) as cnt
        from issue
        group by book_no
    ) t
);




Q42) Write an SQL query to count how many books are issued to Lifetime members.

--select count(i.book_no) as total_books
from issue i
join member m on i.member_id = m.member_id
where m.membership_type = 'lifetime';




Q43) Write an SQL query to display each membership type and number of members in that type.

--select membership_type, count(member_id) as total_members
from member
group by membership_type;




Q44) Write an SQL query to list first 5 members who joined the library.

--select * from member
order by join_date
limit 5;




Q45) Write an SQL query to list members (with membership type) who issued books during
December month.

--select distinct m.member_name, m.membership_type
from member m
join issue i on m.member_id = i.member_id
where month(i.issue_date) = 12;




Q46) Write an SQL query to list members who have not returned books yet.

--select distinct m.member_name
from member m
join issue i on m.member_id = i.member_id
where i.return_date is null;





Q47) Write an SQL query to list members who joined on the same date as 'Garima Sen'.

--select m.member_name
from member m
where m.join_date = (
    select join_date
    from member
    where member_name = 'garima sen'
)
and m.member_name <> 'garima sen';




Q48) Write an SQL query to list members who issued books written by author 'Loni' in
December.

--select distinct m.member_name
from member m
join issue i on m.member_id = i.member_id
join book b on i.book_no = b.book_no
where b.author = 'loni'
and month(i.issue_date) = 12;




Q49) Write an SQL query to list authors whose books are least issued by Lifetime members.

--select b.author
from book b
join issue i on b.book_no = i.book_no
join member m on i.member_id = m.member_id
where m.membership_type = 'lifetime'
group by b.author
having count(i.book_no) = (
    select min(cnt) from (
        select count(i2.book_no) as cnt
        from book b2
        join issue i2 on b2.book_no = i2.book_no
        join member m2 on i2.member_id = m2.member_id
        where m2.membership_type = 'lifetime'
        group by b2.author
    ) t
);






Q50) Write an SQL query to list members who issued books costing more than 300 and written
by author 'Scott Urman'.

--select distinct m.member_name
from member m
join issue i on m.member_id = i.member_id
join book b on i.book_no = b.book_no
where b.price > 300
and b.author = 'scott urman';





Q51) Write an SQL query to list Lifetime members who joined in year 2006 and issued only one
book.

--select m.member_name
from member m
join issue i on m.member_id = i.member_id
where m.membership_type = 'lifetime'
and year(m.join_date) = 2006
group by m.member_id, m.member_name
having count(i.book_no) = 1;





Q52) Write an SQL query to update penalty amount of member 'Garima '.

--update member
set penalty = 500
where member_name = 'garima sen';
